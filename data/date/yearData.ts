const d = new Date()
let currentYear = d.getFullYear()

export const yearData = [
        currentYear - 2,
        currentYear - 1,
        currentYear,
        ++currentYear,
        ++currentYear,
        ++currentYear,
        ++currentYear,
        ++currentYear,
        ++currentYear,
        ++currentYear,
        ++currentYear,
        ++currentYear,
        ++currentYear,
]