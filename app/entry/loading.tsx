import LoadingForm from "@/components/loadingSkeleton/LoadingForm";
import { Skeleton } from "@/components/ui/skeleton";

export default function Loading() {
    return(    
        // <p>loading from /entry...</p>        
        <LoadingForm/>
    )
};